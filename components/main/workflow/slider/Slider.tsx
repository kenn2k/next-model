import { Box } from "@mui/material";

const Slider = () => {
  return (
    <Box marginTop={8}>
      slider
      <Box
        sx={{
          width: 302,
          height: 302,
          borderRadius: 4,
          backgroundColor: "grey.300",
        }}
      />
    </Box>
  );
};

export default Slider;
